apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simplybyte-chart.fullname" . }}-test-connection"
  labels:
    {{- include "simplybyte-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simplybyte-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
