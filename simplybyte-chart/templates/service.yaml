apiVersion: v1
kind: Service
metadata:
  name: {{ include "simplybyte-chart.fullname" . }}
  labels:
    {{- include "simplybyte-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simplybyte-chart.selectorLabels" . | nindent 4 }}
